# Gradle daemon JVM arguments (Java 25+ native access)
org.gradle.jvmargs=--enable-native-access=ALL-UNNAMED -Xmx1g

# Enable configuration cache for faster builds
org.gradle.configuration-cache=true

# Parallel execution and build cache
org.gradle.parallel=true
org.gradle.caching=true
